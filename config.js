/* ============================================================================
   Supabase connection config
   ----------------------------------------------------------------------------
   Leave these blank to run in DEMO mode (ratings save only on this device).
   To go LIVE and shared across all members:
     1. Create a free project at https://supabase.com
     2. Run the SQL in supabase.sql (Supabase dashboard → SQL Editor)
     3. Project Settings → API: copy the Project URL and the anon/public key
     4. Paste them below and commit. That's it — realtime sync turns on.
   The anon key is safe to expose in a public repo: it only allows the
   operations the SQL policies permit (read all ratings, upsert a rating).
   ========================================================================== */
window.SUPABASE_URL = "https://vfxushzugnbnqtcpgcfj.supabase.co";
window.SUPABASE_ANON_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZmeHVzaHp1Z25ibnF0Y3BnY2ZqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODI0ODAzNTksImV4cCI6MjA5ODA1NjM1OX0.jRVrl4A1lYOkpInNxLNU2IrXOhlBas3X1u0-aguLfC4";